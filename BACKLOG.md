# Backlog

Running list of work on war-brief. Newer items at the top. Move done items to the bottom under "Done."

## Next

- [ ] Wire `docs/sources.yaml` into `fetch_rss.py` so feeds are not hardcoded.
- [ ] Load prompts from `prompts/*.md` at runtime instead of inline strings.
- [ ] Add classification step: run `classify_opportunity.md` over each SAM result, then feed only `direct` and `adjacent` items into the synthesis prompt. Saves tokens.
- [ ] Render brief HTML with a mobile-first layout. Dark background, large readable text, no clutter.
- [ ] Add a "yesterday's brief" link on the site for one-click backread.

## Someday

- [ ] Add Hill-side sources: HASC and SASC markups, GAO reports on programs of interest.
- [ ] Track CRS (Congressional Research Service) reports on autonomy and counter-UAS.
- [ ] Pull from FedBizOpps archive for trend analysis (which agencies post what, when).
- [ ] Weekly digest (Friday) that compares this week to last.
- [ ] Sentiment / framing check across press sources on the same story.

## Done

(empty)
